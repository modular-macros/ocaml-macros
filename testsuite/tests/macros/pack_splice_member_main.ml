(* The driver that reads the packed member's spliced value back through
   the pack's name.  Compiled and linked by pack_splice_member.ml. *)

let () = Printf.printf "%d\n" Pack_splice_member_p.Pack_splice_member.v
